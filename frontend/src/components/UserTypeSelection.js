/*
User Selection Page - Borrower or Lender

-- Create Function for the selection to be remembered, and passed to the next page

-- Create a button for each selection (Borrower and Lender)

-- Create a button if user already has an account to go to the login page

-- Create a button that routes to the signup page based on the selection

*/

import React from 'react';

function UserTypeSelection() {
  return (
    <div>
      <h2>Identify Yourself Below</h2>
    </div>
  );
}

export default UserTypeSelection;
